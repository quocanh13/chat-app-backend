components:
  schemas:
    ErrorResponse:
      type: object
      required:
        - error
        - detail
      properties:
        error:
          type: string      
        detail:
          type: object
          additionalProperties:
            type: array
            items:
              type: string
